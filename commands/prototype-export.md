---
description: Validate and export a standalone interactive prototype.
---

Use the `prototype` tool with `action: "export"` for the current repository. If validation prevents exporting, report the errors and do not claim an export was written. Otherwise, report the generated export path.
