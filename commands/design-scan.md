---
description: Scan existing UI sources and propose an unsaved design system.
---

Use the `colophon` tool with `scan: true` to scan the current repository's existing UI. Report the evidence and proposed design system, clearly stating that it is unsaved. Do not write any repository files. Tell the user to open the Colophon canvas to review and save the proposal.
